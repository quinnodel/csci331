# macOS
.DS_Store
._*
.Spotlight-V100
.Trashes

# Editors
.vscode/
.idea/
*.swp

# Node (from Week 9 onward)
node_modules/
npm-debug.log*
.next/
dist/
build/

# Secrets — DB creds for the PHP/MySQL weeks live here, never commit them
.env
.env.*
config.local.php
db_config.php

# Claude Code — settings, agents, skills; nothing under .claude/ is coursework
.claude/

# Agent-facing docs stay local: CLAUDE.md, SETUP.md, SCHEDULE.md, BRIEF.md and
# notes/ are instructions for Claude sessions working in this directory, not
# coursework. Every markdown file is ignored except README.md.
*.md
!README.md
notes/
